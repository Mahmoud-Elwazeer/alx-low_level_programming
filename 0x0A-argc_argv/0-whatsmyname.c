#include <stdio.h>
#include "main.h"

/**
 * main - Entry func
 * @argc: Number of command lines
 * @argv: array for content command lines
 * Return: Always 0
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
