#include <stdlib.h>
#include "main.h"

/**
 * strtow - splits a string into words.
 * @str: string
 * Return: splits a string into words.
 */
char **strtow(char *str)
{
	if (str == NULL || str == '\0')
		return (NULL);
	return (NULL);
}
